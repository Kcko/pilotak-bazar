<div class="LangNav hidden md:block">
	{foreach $data as $language}
		{if !$language['selected']}
			<a href="{$language['url']}" class="text-mobile-menu uppercase text-white">{$language['lang']|upper}</a>
		{/if}
	{/foreach}
</div>
